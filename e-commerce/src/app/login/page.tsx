// will be a controller for the admin page usin next routing standards

import React from "react";
import { Login } from "./loginPage";

export default function LoginPage() {
  return (
    <main>
      <Login />
    </main>
  );
}